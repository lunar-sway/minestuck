name: Build

on:
  push:
    branches: 
      - '1.18'
      - 'upcoming-content'
  pull_request:
    branches: 
      - '1.18'
      - 'upcoming-content'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Validate Gradle wrapper
      uses: gradle/wrapper-validation-action@v1
    - name: Gradle Build
      uses: gradle/gradle-build-action@v2
      with:
        arguments: build
    - name: Data Generation
      uses: gradle/gradle-build-action@v2
      with:
        arguments: :runData
    - name: View light_water
      run: cat src/main/generated/resources/data/minestuck/tags/fluids/light_water.json
    - name: Has File Changes
      uses: anhpham1509/check-uncommitted-changes-action@d6774588ba954c53b7f01b78505d087e1b1a32f1
